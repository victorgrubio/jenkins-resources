version: '3'
services:
  jenkins:
    container_name: jenkins
    image: jenkins/jenkins:2.255
    ports:
      - "8081:8080"
    volumes:
      - "$PWD/jenkins_home:/var/jenkins_home"
    networks:
      - net
    restart: always
networks:
  net:

